using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JunkValue : MonoBehaviour
{

    public int junkValue = 0;
    public float timeValue = 0f;
    // Start is called before the first frame update
}
